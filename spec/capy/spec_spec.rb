# frozen_string_literal: true

RSpec.describe Capy::Spec do
  it "has a version number" do
    expect(Capy::Spec::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
